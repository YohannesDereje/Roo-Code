intent_ledger:
  version: "1.0.0"
  active_intent_id: "INT-001"
  status: "locked"
  intents:
    - id: "INT-001"
      classification: "feature" # Options: feature, refactor, fix
      status: "active"
      metadata:
        task: "Implement Pre-Hook logic"
        owner: "FDE-Lead"
        scope:
          files:
            - "src/core/assistant-messages/presentAssistantMessage.ts"
            - "src/hooks/"
          forbidden:
            - "webview-ui/"
        constraints:
          - "Logic must be placed before the tool execution switch."
          - "Handshake failure must return a string-based Governance Error."
    - id: "REF-002"
      classification: "refactor"
      status: "inactive"
      metadata:
        task: "Cleanup Hook Types"
        owner: "FDE-Lead"
        scope:
          files:
            - "src/hooks/types.ts"
            - ".orchestration/**/*"
          forbidden:
            - "src/core/"
        constraints:
          - "Ensure zero functional changes."
          - "Maintain 100% type coverage."